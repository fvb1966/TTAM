{
  "name": "ttam",
  "version": "0.1.0",
  "private": true,
  "description": "Table Tennis Academy Manager (TTAM) - Electron + Vite + React",
  "main": "dist/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "electron .",
    "build:main": "esbuild src/main/main.ts --bundle --platform=node --outfile=dist/main.js --external:electron --sourcemap && esbuild src/main/preload.ts --bundle --platform=node --outfile=dist/preload.js --sourcemap",
    "build:main:watch": "concurrently \"esbuild src/main/main.ts --bundle --platform=node --outfile=dist/main.js --external:electron --sourcemap --watch\" \"esbuild src/main/preload.ts --bundle --platform=node --outfile=dist/preload.js --sourcemap --watch\"",
    "electron:dev": "concurrently \"npm:dev\" \"npm:build:main:watch\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "test": "vitest"
  },
  "build": {
    "appId": "com.ttam.app",
    "productName": "TTAM",
    "files": [
      "dist/**",
      "package.json"
    ],
    "directories": {
      "buildResources": "build"
    },
    "win": {
      "target": "nsis"
    }
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zustand": "^4.3.6",
    "react-hook-form": "^7.43.9",
    "zod": "^3.22.2",
    "@tanstack/react-table": "^8.8.2",
    "jspdf": "^2.5.1",
    "xlsx": "^0.18.5",
    "@prisma/client": "^5.13.0"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "vite": "^5.2.0",
    "@vitejs/plugin-react": "^4.0.0",
    "electron": "^26.0.0",
    "electron-builder": "^24.4.0",
    "tailwindcss": "^3.4.6",
    "postcss": "^8.4.24",
    "autoprefixer": "^10.4.14",
    "prisma": "^5.13.0",
    "concurrently": "^8.2.0",
    "wait-on": "^7.0.1",
    "vitest": "^1.2.3",
    "esbuild": "^0.19.4",
    "cross-env": "^7.0.3",
    "eslint": "^8.45.0",
    "prettier": "^2.8.8",
    "eslint-config-prettier": "^10.1.8"
  }
}
